/**
 * Contains functionality specific to the login page.
 *
 * @package           Froware
 */

(function( $ ) {
	'use strict';

	$(
		function() {
			$( '.login h1 a' ).attr( 'href', '/' );
		}
	);
})( jQuery );
